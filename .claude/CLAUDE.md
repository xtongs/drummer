# Drummer 项目 AI 助手配置

## 🚨 强制检查门卫（AI 助手必须执行）

**在开始任何代码修改之前，AI 助手必须执行以下检查**：

```markdown
## 变更分类检查

### 1️⃣ 判断变更级别
请向用户确认本次变更属于哪个级别：

- **🟢 微小修改**（Minor）
  - 修复 typo/注释
  - 调整样式/颜色/常量
  - 代码格式化
  - 不改变业务逻辑的重构
  - **流程**：直接实现 → 测试 → 提交

- **🟡 常规功能**（Feature）
  - 新增一个功能点
  - 修改现有功能行为
  - 新增/修改测试用例
  - **流程**：必须有 proposal → 实现 → 验证

- **🔴 重大功能**（Major）
  - 跨多个模块的大型功能
  - 架构层面的变更
  - Breaking changes
  - **流程**：完整 SDD/TDD（proposal → design → tasks → implement → verify → archive）

### 2️⃣ 规范文档检查
- [ ] 如果是"常规功能+"，是否有对应的 `openspec/changes/*/proposal.md`？
- [ ] 如果没有规范，AI 助手必须**拒绝实现**，引导用户先创建提案

### 3️⃣ 测试检查
- [ ] 是否有对应的测试用例？
- [ ] 测试覆盖率是否达到 80%？

### 4️⃣ 规范同步
- [ ] 如果修改了功能行为，是否同步更新了 `openspec/specs/*/spec.md`？
```

**示例对话**：
```
用户：帮我添加一个移动端双击功能

AI：我需要先确认一下这个变更的级别：

这个变更属于：
- 🟢 微小修改（调整现有常量/样式）
- 🟡 常规功能（新增功能点，需要创建 proposal）
- 🔴 重大功能（跨模块/架构变更）

根据您的描述，这属于"常规功能"，我需要先引导您创建 proposal。

在 `openspec/changes/` 中运行 `/proposal` 命令创建提案...
```

---

## 必须遵循的开发规范

本项目使用 **OpenSpec** 进行规范驱动开发（SDD）和 TDD（测试驱动开发）。

### 核心文档

- 📋 **AI 助手指令**: 参见 [AGENTS.md](AGENTS.md) 和 [openspec/AGENTS.md](openspec/AGENTS.md)
- 📖 **项目上下文**: 参见 [openspec/project.md](openspec/project.md)
- 📚 **功能规范**: 参见 [openspec/specs/](openspec/specs/)
- 📝 **变更提案**: 参见 [openspec/changes/](openspec/changes/)

### 开发流程

**完整流程**（重大功能）：
```
提案 → 设计 → 任务 → 实现 → 验证 → 归档
```

**简化流程**（常规功能）：
```
提案 → 实现 → 验证 → 归档
```

**直接修改**（微小改动）：
```
直接实现 → 测试 → 提交
```

### 必须遵守

1. **规范优先（Spec First）**：在写任何代码之前，必须先有明确的规范
2. **变更分级**：根据影响范围选择合适的流程
3. **新功能/功能修改**：必须先在 `openspec/changes/` 创建 proposal（微小改动除外）
4. **TDD 开发**：遵循红-绿-重构流程，先写测试再写代码
5. **测试覆盖率**：核心逻辑覆盖率 >= 80%

详细说明请阅读 [AGENTS.md](AGENTS.md) 和 [openspec/AGENTS.md](openspec/AGENTS.md)
